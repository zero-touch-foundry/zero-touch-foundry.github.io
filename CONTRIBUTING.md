# Contributing

## Local Development

Install dependencies:

```bash
npm install
```

Start dev server with live reload:

```bash
npm start
```

Build for production (also validates the full build):

```bash
npm run build
```

Preview the production build:

```bash
npm run serve
```
